# Index cleanup report (2026-03-03)
Scope: normalize `indexes/MASTER_INDEX.md` into a TOC-only chapter map, and lightly normalize index view files.
## Changes applied
### MASTER_INDEX.md
- Removed deep file listings and per-item registries.
- Kept only chapter folders + links to existing `INDEX_*` view files.
### Normalized view indexes
- INDEX_DRILLS.md: normalized bullet prefixes (e.g., `ap:` → `entry:`) + whitespace cleanup.
- INDEX_SKILLS.md: normalized bullet prefixes (e.g., `ap:` → `entry:`) + whitespace cleanup.

## Notes

## Policy reminder (to prevent future drift)
- MASTER_INDEX.md is TOC-only. Edit it only when the chapter list changes.
- `INDEX_*` files are allowed to be detailed views, but should prefer **skill-family index entrypoints** over deep per-file lists when possible.
