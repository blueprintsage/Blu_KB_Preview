# Character LockPack (Template)

Migrated from legacy KB (`assets/templates/character/lockpack/`).

Canonical location (Preview): `templates/character/lockpack/`

Purpose:
- Portable **Character Lock Pack** output used to reduce drift.
- “Archetype Generator” in legacy wording = *the process that produces a lockpack*, not a separate program.

Use:
- Duplicate template per character id.
- Fill `spec.md` + `rules.md`.
- Add refs under `refs/`.
- Put authored outputs under `outputs/step*/`.
