# ALGORITHMS — Teaching Lane (A)

_Source: Heineman, Pollice, Selkow - Algorithms In A Nutshell, 2nd Ed. - 2016.pdf_

## README

# algorithms-in-a-nutshell-2e

Source: Heineman, Pollice, Selkow - Algorithms In A Nutshell, 2nd Ed. - 2016.pdf

This pack was generated via PASS:GUT-LADDER in Dual-Lane format.

## SYLLABUS

## Syllabus

1. Thinking & analysis
2. Building blocks
3. Sorting
4. Searching
5. Graphs
6. Strings
7. Geometry/spatial trees
8. Testing & benchmarking

## PATTERNS

# Patterns

## PAT-001 — Start with a naïve solution, then optimize with constraints
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-002 — Choose data structure first; algorithm follows
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-003 — Analyze best/average/worst-case separately when it matters
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-004 — Use divide-and-conquer to reduce problem size
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-005 — Trade time for space with caching/memoization
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-006 — Prefer in-place algorithms when memory pressure is high
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-007 — Use heap when you need repeated min/max extraction
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-008 — Use partitioning when you need selection or quicksort-like behavior
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-009 — Use hashing for expected O(1) membership/lookup
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-010 — Use balanced BST when you need ordered operations
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-011 — Model as a graph when relationships dominate; then pick traversal
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-012 — Use BFS for shortest path in unweighted graphs
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-013 — Use Dijkstra/A* when edge weights matter
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-014 — Use union-find for dynamic connectivity
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-015 — Use dynamic programming for optimal substructure + overlapping subproblems
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-016 — Use string matching automata when repeated searches on large texts
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-017 — Use sweep-line / plane partitioning patterns for geometric problems
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## PAT-018 — Use spatial trees (quadtree/R-tree) for range queries and nearest neighbors
- IF: You face a problem matching this pattern in software.
- THEN: Apply the described strategy; validate with complexity + constraints; benchmark if uncertain.

## DRILLS

# Drills

## DRL-001 — Given constraints, pick the right sort (stable/in-place/linear-time).
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-002 — Implement binary search variants (first/last occurrence).
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-003 — Benchmark two implementations and report cross-over point.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-004 — Convert a problem statement into an input-size model n and identify dominant term.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-005 — Implement a heap-based priority queue and use it for top-k.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-006 — Run BFS and DFS on the same graph and compare discovery/finish orders.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-007 — Implement Dijkstra on adjacency list; verify with a small weighted graph.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-008 — Add an admissible heuristic and compare A* expansions vs Dijkstra.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-009 — Write DP recurrence for edit distance; test on 5 pairs.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-010 — Implement union-find with path compression; test random unions/finds.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-011 — Implement KMP prefix function; find matches in a long string.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-012 — Build a quadtree for points; answer range query and nearest neighbor.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-013 — Create 10 randomized tests for a sorting routine (property-based).
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## DRL-014 — Design a rejection test: detect when an algorithm’s assumptions are violated.
- IF: You are studying or validating algorithm selection/implementation.
- THEN: Complete the task; record results; note time/space and failure modes.

## TESTS

# Tests

See Lane B `skills/.../tests/tests.jsonl`.
